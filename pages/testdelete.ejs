<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>TEST APPLICATION</title>
    </head>
    <body>
        <script src="/main.js"></script>
        <h1>MONGODB CRUD OPERATIONS TEST</h1>
        <p>
           <ul>
               <% for(var i = 0; i < names.length; i++) { %>
                <li>
                    <span> <%= names[i].name %> <button id="<%=names[i].name %>" onclick="clicked(id)" > delete </button> </span>
                </li>
                <% } %>
           </ul>
        </p>
        <p><a href="/">Go back</a></p>
    </body>
</html>